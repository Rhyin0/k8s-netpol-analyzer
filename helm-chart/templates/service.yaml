apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-analyzer
  namespace: {{ .Release.Namespace }}
  labels:
    app: netpol-analyzer
spec:
  selector:
    app: netpol-analyzer
  ports:
    - name: metrics
      port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
